#!/usr/bin/env node

var program  = require('commander');

program
	.version('<%= appVersion %>')
	.on('*', function(name) {
		console.log('\''+name+'\' is not a known command. See \'<%= appName %> --help\':');
		program.outputHelp();
		process.exit(1);
	});

<%= components %>

program
	.parse(process.argv);

if (program.args.length < 1 ) {
	console.log('No command specified. See \'<%= appName %> --help\':');
	program.outputHelp();
	process.exit(1);
}