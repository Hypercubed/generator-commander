/* <%= compName %> commander component
 * To use add `require('../lib/<%= compSlug %>.js')(program, opts)` to your commander.js based node executable before `program.parse`.
 */
'use strict';

module.exports = function(program, opts) {

	<%= code %>
	
};