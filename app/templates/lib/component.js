/* <%= name %> commander component
 * To use add `require('../lib/<%= slugname %>.js')(program, opts)` to your commander.js based node executable before `program.parse`.
 */
'use strict';

module.exports = function(program, opts) {

	<%= code %>
	
};